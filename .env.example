# Confluence publish (copy to .env and fill in — never commit .env)
CONFLUENCE_BASE_URL=https://yoursite.atlassian.net/wiki
CONFLUENCE_EMAIL=you@company.com
CONFLUENCE_API_TOKEN=
CONFLUENCE_PAGE_ID=

# Optional — branch for GitHub raw URLs in the markdown doc (default: main)
# CONFLUENCE_IMAGE_BRANCH=main
